# compilance-clerk
The Compliance Clerk is a Python-based backend system that extracts structured data from PDF documents like eChallan and NA permissions. It uses OCR fallback, processes text into JSON, exports to CSV, and maintains audit logs for reliable document data extraction.
